# name: PRs reviews reminder

# on:
#   schedule:
#     # - cron: "*/5 * * * *"  # Every 5 minutes
#   workflow_dispatch:  

# jobs:
#   pr-reviews-reminder:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Send PR reminder to Slack
#         uses: davideviolante/pr-reviews-reminder-action@v2.8.0
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           webhook-url: ${{ secrets.SLACK_WEBHOOK_TEST }}
#           provider: 'slack'
#           channel: '#devops-trash' 
#           github-provider-map: 'tonia-kas:U07KNA1ADBQ'
